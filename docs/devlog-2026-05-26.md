# Dev Log — May 26, 2026
**Project:** SecureSchedule App  
**Session Duration:** ~2 hours  
**Developer:** Kiet

---

## Summary
First development session for the SecureSchedule App — a full-stack PWA scheduling application built as a cybersecurity and AWS portfolio project.

---

## What Was Accomplished

### Environment Setup
- Installed **Node.js v24.15.0** (LTS) via Windows .msi installer
- Fixed PowerShell execution policy (`Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`)
- Installed **Claude Code v2.1.129** via native Windows installer (`irm https://claude.ai/install.ps1 | iex`)
- Added `C:\Users\Kiet\.local\bin` to Windows PATH for Claude Code
- Installed **PostgreSQL 18** via Windows installer, including pgAdmin 4
- Added `C:\Program Files\PostgreSQL\18\bin` to Windows PATH for psql

### Project Scaffolding (via Claude Code)
Scaffolded a full-stack PWA with the following structure:
- `frontend/` — React app with mobile-first responsive design, PWA manifest and service worker
- `backend/` — Node.js/Express REST API
- `backend/db/schema.sql` — PostgreSQL schema
- `backend/.env.example` — environment variable template

**Frontend pages built:** Login, Register, Dashboard (weekly calendar view), Add/Edit event

**Security features implemented:**
- bcrypt password hashing
- JWT authentication with httpOnly cookies
- Input validation on all endpoints
- Rate limiting on login endpoint
- Activity logging (login, register, created event, edited event, deleted event)
- HTTPS-ready (enforced in production)

**Database tables created:** users, schedule_items, activity_logs

### Database Setup
- Generated JWT secret using PowerShell random string generator
- Created `.env` file with real PostgreSQL credentials and JWT secret
- Created `secure_schedule` database via psql
- Ran `schema.sql` to create all tables, functions, triggers, and indexes

### Git & GitHub
- Initialized git repo and pushed to GitHub
- Fixed accidental commit of `node_modules` and `.env` — removed from tracking via `git rm --cached`
- Added `.gitignore` covering `node_modules/`, `.env`, `*.env`, with exception for `.env.example`
- Set upstream branch: `git push --set-upstream origin main`

### Testing
- Registered a test account — confirmed bcrypt hashing and activity logging working
- Added test schedule events — confirmed database writes and activity log entries
- Fixed a date comparison bug (DATE columns returned as strings) — committed and pushed
- Verified weekly calendar view renders correctly after bug fix

---

## App Stack
| Layer | Technology |
|---|---|
| Frontend | React, PWA (manifest + service worker) |
| Backend | Node.js, Express, nodemon |
| Database | PostgreSQL 18 |
| Auth | JWT (httpOnly cookies), bcrypt |
| Security | Input validation, rate limiting, activity logging |
| Version Control | Git, GitHub |

---

## Known Issues / To Do Next Session
- [ ] Fix npm vulnerabilities (5 total: 3 moderate, 2 high) — run `npm audit fix`
- [ ] Purge `.env` and `node_modules` from full git history (not just current tracking)
- [ ] Add `.gitattributes` for Windows line ending handling
- [ ] Set up AWS deployment (EC2, RDS, S3, CloudFront, Secrets Manager, CloudWatch)
- [ ] Test PWA install on iPhone via Safari
- [ ] Write README with architecture diagram

---

## Commands Reference (for next session)

**Start the app locally:**
```powershell
# Terminal 1 - Backend
cd "C:\Users\Kiet\projects\personal\secure-schedule-app\backend"; npm run dev

# Terminal 2 - Frontend
cd "C:\Users\Kiet\projects\personal\secure-schedule-app\frontend"; npm start
```

**Open app:** http://localhost:3000

---

## Notes
- Password validation intentionally allows all printable characters (follows NIST SP 800-63B)
- Activity log captures IP address, action type, and timestamp for all user events
- nodemon auto-reloads backend on file save during development
